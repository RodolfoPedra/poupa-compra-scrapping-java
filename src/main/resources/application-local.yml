spring:
  cloud:
    openfeign:
      client:
        config:
          poupa-compra-api:
            url: http://localhost:8182
            connectTimeout: 5000
            readTimeout: 5000
            loggerLevel: full